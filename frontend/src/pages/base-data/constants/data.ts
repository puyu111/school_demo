/**
 * base-data 模块 - 业务数据常量
 * 定义各步骤页面使用的默认数据和选项列表
 */

/**
 * 课程设置步骤 - 默认课程名称列表
 * 生成 20 个默认课程名称用于下拉选择
 */
export const DEFAULT_COURSE_NAMES = Array.from({ length: 20 }).map(
  (_, i) => `课程 ${i + 1}`,
);

/**
 * 专业设置步骤 - 可选课程列表
 * 定义专业可选择的所有课程名称
 */
export const MAJOR_COURSES = [
  '高等数学',
  '线性代数',
  '概率论',
  '大学物理',
  '计算机基础',
  '数据结构',
  '算法设计',
  '软件工程',
  '数据库原理',
  '网络技术',
  '操作系统',
  '编译原理',
  '计算机网络',
  '软件测试',
  '人工智能',
] as const;

/**
 * 教师录入步骤 - 可授课程列表
 * 生成 10 个默认课程名称用于教师可授课程选择
 */
export const TEACHER_COURSES = Array.from({ length: 10 }).map(
  (_, i) => `课程 ${i + 1}`,
);
